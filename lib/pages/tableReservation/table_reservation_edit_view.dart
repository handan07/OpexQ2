import 'package:flutter/material.dart';

class TableReservationEditView extends StatelessWidget {
  const TableReservationEditView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        //appBar: const AppBarWidget("Restoran Reservation"),
        body: SingleChildScrollView(child: Container()));
  }
}
