import 'package:flutter/material.dart';

class ReservationEditView extends StatelessWidget {
  const ReservationEditView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        //appBar: const AppBarWidget("Reservation Edit"),
        body: SingleChildScrollView(child: Container()));
  }
}
