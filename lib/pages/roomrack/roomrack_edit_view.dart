import 'package:flutter/material.dart';

class RoomrackEditView extends StatelessWidget {
  const RoomrackEditView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        //appBar: const AppBarWidget("Approval Edit"),
        body: SingleChildScrollView(child: Container()));
  }
}
